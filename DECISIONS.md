# DECISIONS

아키텍처·설계 결정 기록. 새 결정은 위에 추가한다.

## 2026-08-06

### 1. 카톡은 티저 + 링크, 본문은 GitHub Pages
카카오 기본 텍스트 템플릿이 200자 제한이라 브리핑 전문을 담을 수 없다.
카톡에는 날짜 + 한 줄 요약 + 헤드라인 + Pages 링크만 보내고, 본문은 정적 HTML로 발행한다.

### 2. 주식/부동산 완전 분리 발송·분리 워크플로우
- 카톡 2통 (주식 08:40 / 부동산 08:50 KST), 각각 독립 200자 + 독립 링크
- 워크플로우 파일 분리 (`brief-stock.yml` / `brief-realestate.yml`)
- 사유: 한쪽만 재실행·중지 가능해야 하고, Actions 로그를 도메인별로 봐야 디버깅이 된다.
  한 도메인 실패가 다른 도메인에 전파되지 않아야 한다 (allSettled, 독립 커밋).
- Pages 커밋 충돌 방지: 10분 간격 + 커밋 전 rebase
- 파이프라인 로직은 `runPipeline(domain)` 하나로 통일, 도메인 차이는 설정 객체 주입

### 2-1. 카카오 발송 쿼터 확인 결과 — 하루 2통은 안전
공식 쿼터 문서 확인(2026-08-06): 카카오톡 메시지 API는 **발신자당 일 100건**,
수신자당 일 100건, 발신자/수신자 pair당 일 20건.
"나에게 보내기"는 발신자=수신자=본인이므로 **pair당 20건이 실질 상한**이다.
하루 2통(주식·부동산)은 상한의 10%로 충분히 여유 있고, 수동 재실행도 감당된다.
쿼터 상향은 불가하므로 향후 도메인을 늘려도 pair 20건을 넘기지 않게 설계할 것.

### 2-2. 링크를 본문(text) 안에 넣는다
Pages URL이 약 62자로 200자 한도의 31%를 차지한다.
카카오 텍스트 템플릿의 `link` 필드만 쓰고 본문에서 URL을 빼면 그만큼 헤드라인을 더 담을 수 있으나,
카톡에서 링크가 있다는 사실이 시각적으로 드러나지 않아 넘어가는 동선이 약해진다.
사용자 지정 포맷대로 본문에 `→ 링크`를 유지하고, `link` 필드도 함께 채워 메시지 전체를 탭 가능하게 한다.

### 3. 시세 소스는 네이버 증권 비공식 API 단일
2026-08-06 실측: Yahoo Finance chart API는 비인증 요청에 상시 429, stooq CSV도 사용 불가.
네이버(`api.stock.naver.com`, `m.stock.naver.com`)가 미국지수·국내지수·환율을 모두 커버함을 확인.
비공식이라 예고 없이 깨질 수 있으나 폴백 없이 단일 소스로 가고, 실패 시 "데이터 없음" 처리로 감수한다.

### 8. 러셀2000 제외
2026-08-06 실측: 네이버는 `.RUT`/`RUT`/`^RUT`/`RUI` 모두 "지원하지 않는 지수"(409),
stooq CSV는 사용 불가, Yahoo는 429. IWM(ETF)도 네이버 마스터에 없다.
ETF를 지수 대용으로 쓰면 "수집데이터에 없는 숫자를 만들지 않는다"는 프롬프트 규칙에 어긋나므로
러셀2000은 수집 항목에서 뺀다. 무료 소스가 확인되면 `US_INDEX_CODES`에 추가만 하면 된다.

### 9. 시장지표 코드 확정 (실측)
네이버 시장지표는 `marketIndex/productDetail?category=&reutersCode=` 조합이며 코드가 문서화돼 있지 않다.
탐색으로 확정: 미10년물 `bond` / `US10YT=RR`, 달러인덱스 `exchange` / `.DXY`, 원/달러 `exchange` / `FX_USDKRW`.
지수와 달리 변동폭 필드명이 `fluctuations`다 (지수는 `compareToPreviousClosePrice`).

### 10. Reddit 커뮤니티 수집 보류 (2026-08-06)
사용자에게 Reddit 계정이 없어 script 앱을 만들 수 없다.
`community.ts`는 만들지 않고, 브리핑의 [커뮤니티] 항목은 "데이터 없음"으로 나간다.
계정이 생기면 #4의 설계 그대로 추가하면 된다 (config의 redditConfig는 이미 있음).

### 11. Gemini 모델은 gemini-3.6-flash (2026-08-06)
`gemini-2.5-flash`·`gemini-2.5-flash-lite`는 "no longer available to new users"로 404,
`gemini-2.0-flash`는 429(무료 쿼터 없음). 실측으로 동작을 확인한 최신 flash 계열을 쓴다.
모델명은 env(`GEMINI_MODEL`) 주입이라 코드 변경 없이 교체 가능하다.

### 4. Reddit은 OAuth script 앱으로 접근
2026-05부로 비인증 `.json` 엔드포인트가 차단됨(403, 실측 확인).
무료 script 앱 OAuth로 전환. 인증해도 Actions 공유 IP가 차단될 가능성이 있어
community 수집기는 실패 허용 소스로 설계한다.

### 5. 실적 가이던스·컨콜 발언은 뉴스 기사에 의존
발표치·컨센서스·서프라이즈는 Nasdaq 비공식 API로 구조화 수집이 가능하지만,
가이던스와 컨콜 발언을 주는 무료 구조화 소스는 없다.
뉴스 RSS 기사를 수집데이터로 넣고 요약 프롬프트가 추출하게 한다. (사용자 승인: 2026-08-06)

### 6. config는 그룹 단위 검증
전체 env를 한 번에 강제하면 로컬에서 수집기만 테스트하는 단계(3단계)에도
카카오·Gemini 키가 필요해진다. 모듈별로 자기 그룹만 로드하고,
누락 시 해당 그룹 최초 접근 시점에 명확한 에러로 죽는다.

### 7. 브리핑은 공개 URL에 노출됨을 감수
무료 GitHub 계정은 public 레포만 Pages를 쓸 수 있다.
브리핑에 개인정보가 없으므로 public으로 간다.

### 12. 투자자 순매수는 네이버 금융 HTML (KRX 제외)
KRX 정보데이터시스템(`data.krx.co.kr/comm/bldAttendant/getJsonData.cmd`)은
세션 쿠키를 먼저 받아 넣어도 `LOGOUT` 문자열만 반환한다 (2026-08-06 실측, bld 3종 시도).
대신 네이버 금융 투자자별 매매동향(일별) HTML을 파싱한다. 단위 억원.
`bizdate` 파라미터는 필수 — 빼면 빈 표가 온다. 공휴일 대비로 최근 거래일 후보를 순차 시도한다.
종목별 순매수 상위 3종목은 이 페이지에 없어 이번 범위에서 제외했다.

### 13. 실적 실제치는 발표 다음 날에야 채워진다
Nasdaq `earnings-surprise` 표는 발표 직후 갱신되지 않는다.
KST 08:40은 미 동부 전일 19:40이라, 장 마감 후 발표분의 실제 EPS는 대체로 아직 없다.
따라서 실적 섹션의 실제 발표치·서프라이즈는 뉴스 기사가 주 근거가 되고,
`epsForecast`(컨센서스)만 구조화 데이터로 확보된다.

### 14. 네이버 검색 API는 NCP NAVER API HUB로 이관됨
2026-08-06 실측: 구 방식(`openapi.naver.com/v1/search/news.json` + `X-Naver-Client-*` 헤더)은 401.
현재는 NAVER Cloud Platform의 NAVER API HUB를 쓴다.
  호스트: https://naverapihub.apigw.ntruss.com
  경로:   /search/v1/news
  헤더:   X-NCP-APIGW-API-KEY-ID / X-NCP-APIGW-API-KEY
호스트·경로·헤더가 전부 바뀌었으므로 셋을 함께 봐야 한다.
키는 NCP 콘솔 > NAVER API HUB > Application에서 발급한다.

### 15. 카톡은 티저가 아니라 분할 발송 (#1 수정)
200자 티저 한 통으로는 헤드라인 3개도 못 담는다 (링크가 200자의 32%를 차지).
브리핑 본문을 200자 단위로 쪼개 최대 4통까지 순서대로 보낸다 (최대 약 800자).
  - 통 순서는 프롬프트 블록 순서: 요약 → 지표 → 어제 스레드 → 이슈 → 일정 → 내일 질문
  - 링크는 마지막 통에만, 절대 자르지 않는다 (자리가 없으면 본문 줄을 덜어낸다)
  - 구획 표시([이슈] 등)가 통 끝에 홀로 남지 않게 다음 통으로 넘긴다
  - 긴 문장은 문장 끝에서 쪼개 이어붙인다 — 통째로 밀면 빈자리가 생긴다
  - 통수가 모자라면 뒤를 자른다. 앞쪽 블록의 중요도가 높다.
쿼터는 pair당 일 20건이라 주식 4 + 부동산 4 = 8건, 재실행 여유를 남긴 값이다.
800자를 채우려고 내용을 늘려 쓰지 않는다 — 모자라면 그만큼만 보낸다.

### 16. 뉴스는 최신성을 강제한다
소스마다 기본 정렬이 달라(구글=관련도, 네이버=날짜) 오래된 기사가 섞였다.
  - 구글뉴스 쿼리에 `when:1d` 연산자를 붙여 24시간으로 제한
  - 수집 후 발행시각 기준 내림차순 정렬 + 30시간 이내만 유지
  - 단 5건 미만으로 줄면 필터를 해제한다 (조용한 날 0건 방지)

### 17. 뉴스는 제목만으로 부족하다
2026-08-06 샌디스크 사례: 실적은 호조였는데 다음 분기 가이던스 때문에 주가가 빠졌다.
제목만 수집하니 요약이 "실제치는 집계되지 않았다"로 처리했다.
기사 요약(네이버 `description`, 구글 RSS `description`)을 함께 수집하고,
프롬프트에 "수집 사정을 브리핑에 쓰지 말 것", "epsForecast는 다음 분기 컨센서스지 발표치가 아님"을 명시했다.

### 18. 카톡 본문에서 링크 제거, 버튼으로 대체 (#15 수정)
링크가 200자의 32%를 차지해 본문을 잡아먹었다.
마지막 통의 `button_title`("브리핑 전문")이 같은 역할을 하므로 본문에서 URL을 뺐다.
템플릿의 `link` 필드는 여전히 필수라 전 통에 채운다 — 빼면 400이 난다.

### 19. 지표 블록은 통 경계에서 쪼개지 않는다
지표가 (1/4)(2/4)로 끊기면 표로 읽히지 않는다.
블록에 atomic 플래그를 두고, 통째로 들어갈 자리가 없으면 새 통에서 시작한다.
한 통(약 185자)에 들어가게 카톡용 라벨을 줄이고(S&P500→S&P, 미10년물→10Y 등)
천단위 콤마를 빼고 그룹당 한 줄로 합쳤다. HTML은 원래 라벨·포맷을 유지한다.
2통 이후 머리글이 짧다는 점을 이용해 예산을 잡고, 1통만 넘치면 2통으로 넘긴다.

### 20. 일정을 브리핑 맨 앞으로, 예정 일정 추가
장 열리기 전 브리핑에서 "오늘 뭘 봐야 하나"가 먼저다.
HTML 순서: 지표 → 오늘 일정 → 예정 일정 → 어제 스레드 → 이슈 → 내일 질문.
  - calendar.ts: thisweek + nextweek를 함께 받아 오늘 이후 14일 High 지표를 `upcoming`으로
  - earnings.ts: 이후 10일간 발표 예정을 날짜별 상위 4종목(워치리스트 우선)으로

### 21. Gemini 무료 티어 RPD가 모델마다 다르다
`gemini-3.6-flash`는 하루 20건이다 (2026-08-06 실측, 429 응답의 quotaValue).
일상 운영(도메인당 1~2콜)엔 충분하지만 개발 중 반복 실행으로 쉽게 소진된다.
`gemini-3.5-flash`로 전환했다. 모델명은 env 주입이라 코드 변경 없이 바꿀 수 있다.

### 22. 부동산 소스 확정 (2026-08-06 실측)
- **price**: 한국부동산원 R-ONE 오픈API. `SttsApiTblData.do`
  주간 매매 `T244183132827305`, 주간 전세 `T247713133046872` (DTACYCLE_CD=WK).
  API는 **지수만 주고 변동률은 주지 않아** 최신 주/직전 주를 받아 코드에서 계산한다.
  지역명은 `서울`/`수도권`/`지방권`. 데이터 없는 주차는 `{"RESULT":{...}}`로 배열이 안 온다.
  발표가 주 1회라 조사 기준일과 `daysAgo`를 함께 넘겨 "n일 전 기준"을 표기하게 한다.
- **chungyak**: 공공데이터포털 청약홈 분양정보 + 경쟁률.
  서비스키는 **Encoding 키를 그대로 쿼리에 붙인다**. 다시 인코딩하거나
  `Authorization: Infuser` 헤더로 보내면 -4(등록되지 않은 인증키)가 난다.
  값이 없는 칸을 null로 주므로 스키마를 nullable로 둬야 한다.
- **news**: 구글뉴스 + 네이버, 부동산 키워드로 도메인 분기 (`createNewsCollector`)
- **supply(미분양·인허가)**: 무료 API를 확정하지 못했다. 이번 범위에서 제외.
- **서울 실거래 건수 / 경매 낙찰가율**: 무료 소스 미확정. 대시보드에 "-"로 자리만 유지한다.

### 23. 청약 도메인 신설 (13:00 KST)
사용자 맞춤 청약 분석을 별도 도메인으로 분리했다. 주식·부동산과 같은 runPipeline을 쓴다.
- `applyhome.ts`: 청약홈 5개 API 통합 (APT 분양 / 무순위·잔여 / 오피스텔·도시형 + 경쟁률 2종).
  청약홈 집계라 LH·SH·GH 등 공급기관 공고가 모두 포함된다.
  정렬은 무순위 → APT → 오피스텔 순 — 통장 순위 무관 물량이 사용자 우선순위 1순위이고,
  아파트가 오피스텔보다 항상 앞이다.
  **접수일 필드명이 유형마다 다르다**: APT는 `RCEPT_*`, 무순위/잔여는 `SUBSCRPT_RCEPT_*`.
  이걸 놓쳐 서울 공고 3건이 통째로 누락됐었다 (3건 → 9건으로 회복).
  신청 링크는 `HOUSE_MANAGE_NO`+`PBLANC_NO`로 청약홈 상세 URL을 조립한다.
- `profile.json`: 사용자 프로필을 코드가 아닌 데이터로 분리. 프롬프트에 `{{PROFILE}}`로 주입.
- 분양가·대출조건·자산기준은 API에 없다. 추정 금지, "공고 확인 필요"로 남긴다.

### 24. HTML에 표 렌더링 추가
청약 분석은 판정·수치가 많아 산문으로는 안 읽힌다.
마크다운 파이프 표(`| a | b |`)와 `[텍스트](URL)` 링크만 인식하는 최소 렌더러를 넣었다.
마크다운 파서를 의존성으로 붙이지 않은 이유는 이 두 문법 외에는 쓰지 않기 때문이다.
링크는 http(s) 스킴만 앵커로 만든다 — 모델 출력이 그대로 DOM에 들어가므로 제한이 필요하다.
넓은 표는 페이지가 아니라 표 자신이 가로 스크롤한다.

### 25. 부동산 대시보드에 물량 지표 추가
`supply.ts`: R-ONE 월간 통계에서 공급물량(신규 분양세대수 `T244633134461863`),
입주물량(주택준공실적 `T237273130004614`), 미분양(`T237973129847263`)을 전국 기준으로 받는다.
월간이라 발표 지연이 커서 최근 달부터 거슬러 올라가며 값이 있는 달을 찾고 기준 연월을 함께 넘긴다.
경매 낙찰가율은 여전히 무료 소스를 찾지 못해 "-"로 자리만 유지한다.

### 26. Gemini 모델 폴백 체인
모델별 무료 일일 한도가 제각각이고(3.6-flash는 20건) 소진되면 그날 브리핑이 통째로 죽는다.
`GEMINI_MODEL`을 쉼표 목록으로 받아 429(일일 한도)를 만나면 다음 모델로 넘어간다.
429가 아닌 오류는 폴백하지 않는다 — 프롬프트나 키 문제를 조용히 덮으면 안 된다.
현재 체인: gemini-3.5-flash → 3.5-flash-lite → 3.1-flash-lite → 3-flash-preview.
도메인이 3개(주식·부동산·청약)로 늘어 하루 최소 3콜, 재작성까지 최대 6콜이 필요하다.
